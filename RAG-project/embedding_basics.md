# Embedding

* is the transformation of sentences or words(information) in higher dimensional vectors
* for example both sentences "lovely cat" and "pretty cat" produce almost same vector because both are semantically similar
* but how can we convert these sentences in to embedding vectors ?